"use client";
import PanelBase from "./_panel-base";
export default function Product({ section, productName, tx }) {
  return <PanelBase section={section} productName={productName} productId="panel-only-aimbot" tx={tx} />;
}
